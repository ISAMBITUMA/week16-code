pipeline {
    agent any
    tools {
        maven 'M2_HOME'
    }
    stages {
        stage('All Maven Commands') {
            steps {
                sh 'mvn clean install compile test package'
            }
        }
    }
}
